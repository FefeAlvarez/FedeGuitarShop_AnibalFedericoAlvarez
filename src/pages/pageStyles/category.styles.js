import { colors } from "../../globals.styles";
const button = {
  textDecoration: "none",
  color: colors.orange,
  padding: "2em",
  margin: 5,
};
const link = {
  textDecoration: "none",
  color: colors.orange,
};

export const styles = {
  button,
  link,
};
